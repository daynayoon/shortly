package com.shortly.service;

public class AnalyticsService {
    // getStats(shortCode, User) → AnalyticsResponse <- get analytics data for a URL if it belongs to the user (total clicks, unique clicks, referrers, user agents)
}
