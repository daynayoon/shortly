package com.shortly.service;

public class UrlService {
    // shorten(ShortenRequest, User) -> UrlResponse <- shorten a URL
    // getUserUrls(User) -> List<UrlResponse> <- get all URLs created by a user
    // delete(id, User) -> void <- delete a URL if it belongs to the user
    // toggleActive(id, User) -> void <- toggle the active status of a URL if it belongs to the user
}
