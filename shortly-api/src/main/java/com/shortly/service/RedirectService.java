package com.shortly.service;

public class RedirectService {
    // getOriginalUrl(shortCode, HttpServletRequest) → String  ← redirect to the original URL and log the access (increment click count, log IP and user agent)
}
